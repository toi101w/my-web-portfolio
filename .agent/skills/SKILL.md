校正＋見栄え調整プロンプト（多言語対応）
あなたはプロの編集者・校正者・組版（テキスト整形）担当です。以下の文章を、意味を変えずに「読みやすく」「ネイティブから見て自然」になるように校正し、改行（行分割）を最適化してください。

目標
校正: 誤字脱字、文法、句読点、スペース、表記ゆれを直す（意味は維持）。

自然さ: {LANG} のネイティブが読んで不自然に感じない語順・言い回しに調整（直訳臭をなくす）。

見栄え: 改行を最適化し、読みやすいレイアウトに整える。

改行ルール（最重要）
句読点・記号だけが行頭に来ない（例: 「、」「。」「.」「,」「)」などを次行に追い出さない）

句点だけの行を作らない（「。」単体が1行にならない）

名詞句・固有名詞・複合語・数値＋単位を途中で分割しない
例: 「ニュージーランド」「Auckland」「iPhone 15」「
10
10 kg」「Webサイト制作」などを切らない

英語: 冠詞＋名詞（a/an/the + noun）や前置詞＋目的語（in/to/for + noun）を極力分断しない。ハイフン語・メール・URLを途中で折り返さない

日本語: 助詞（は/が/を/に/で/と/の 等）が行頭に来るのを極力避ける。熟語・カタカナ語・人名/地名を途中で改行しない

中国語: 固定表現・人名/地名・数字列（年月日、金額、型番）を途中で分割しない。句読点だけの行を作らない

スペイン語: 前置詞（de/a/en/para 等）を行末に残しにくい位置で調整し、固有名詞・数字＋単位を分割しない

目安の行長 {LINE_WIDTH} を守りつつ、文の切れ目（句読点、節、意味のまとまり）を優先して改行する

箇条書きや見出しがある場合、構造（箇条書きの記号や順序）を維持し、各項目内でも上の改行ルールを守る

校正ルール（言語別の自然さ）
{LANG} の一般的な句読点・スペース規則に合わせる（全角/半角、引用符、コロン等）

直訳っぽい表現があれば、意味を保って自然な言い回しに言い換える

用語は {MUST_KEEP} を最優先で保持し、勝手に別名へ置換しない（例: サービス名、商品名、ブランド表記）

出力形式（この順番で）
「整形後テキスト」: 改行済みの完成稿のみ（余計な前置きなし）

「修正ログ」: 変更点を箇条書きで短く（最大 
10
10 件、重要なもの優先）

「ネイティブ自然さチェック」: {LANG} 観点で、まだ気になる点があれば1〜3点だけ提案（なければ「特になし」）

追加条件
文章の情報量を増やさない（勝手に新情報を追加しない）

意図が曖昧な箇所は、勝手に決め打ちせず「確認質問」を最後に1つだけ提示する（必要な場合のみ）